# Open questions — decryption (unresolved after doc dumps + ?ask= queries, 2026-07-03)

1. **Sepolia public-decryption latency** — no figure anywhere (?ask q1, q8 both: "cannot find"). Only adjacent datum: check-balances.md's "2-5 second FHE decryption" for user-decrypt. → Measure empirically on Sepolia before fixing epoch/liquidation timing assumptions.
2. **Max handles per `publicDecrypt` call / relayer payload limits** — undocumented (?ask q4). Only 429/`RelayerRequestFailedError` semantics are documented. → Empirically probe batch sizes for epoch aggregates; keep per-request batches small and chunkable.
3. **`decryptionProof` byte layout** (how many KMS signatures, metadata framing) — not specified in any fetched page; treat as opaque bytes produced by the SDK and consumed by `FHE.checkSignatures`.
4. **Exact removed-API signatures** (`FHE.requestDecryption`, old `checkSignatures(requestId, ...)` form, `DecryptionOracle` address) — all archived doc channels were rewritten to the new model, so the old signatures are no longer documented anywhere; only the changelog note (via ?ask q3) confirms deprecation in FHEVM v0.9. Irrelevant for new code; relevant only if migrating pre-v0.9 samples.
5. **`checkSignatures` "signature caching" gas optimization** — functions.md says it is "optimized for gas via signature caching" vs the view variant, but the caching semantics (per-proof? per-signer? storage growth?) are not described.
6. **`makePubliclyDecryptable` return type discrepancy** — oracle.md declares `internal;` (no return) per-type, functions.md declares generic `internal returns (T)`. Examples never use a return value. Minor; verify against `@fhevm/solidity` source when scaffolding.
7. **Exact v3 permit EIP-712 struct** (domain, type name, fields) — the v3 SDK abstracts it entirely; only the legacy/relayer-sdk flow exposes `UserDecryptRequestVerification` (domain/field list not dumped). Only matters if signing permits without the SDK (e.g. smart-contract wallets; note `skipBalanceCheck: true` exists for wallets that cannot sign EIP-712).
8. **Core-SDK public-decrypt method path** — React `useDecryptPublicValues` and type `DecryptPublicValuesResult` are confirmed; the core method is referred to as `decryptPublicValues` (zamasdk.md hint, alpha guide) but its dedicated reference page was not fetched — presumed `sdk.decryption.decryptPublicValues(handles)`. Verify in `@zama-fhe/sdk` typings.
9. **Sepolia deployment specifics** — KMSVerifier/ACL addresses and the KMS signer threshold value on Sepolia live in https://docs.zama.org/protocol/protocol-apps/addresses/testnet/sepolia.md (not fetched in this pass; fetch when wiring deployments).
10. **Whether `hre.fhevm.publicDecrypt` also works against real Sepolia from Hardhat** (outside mock) — example suites explicitly refuse to run on Sepolia; the production path shown in oracle.md uses `createInstance()`/`instance.publicDecrypt` ("see Zama SDK for exact naming") — which npm package currently ships `createInstance` (relayer-sdk vs sdk v3) is not pinned down by the fetched pages.
11. **Foundry decryption testing** — `development-guide/foundry/write_test.md` and `forge-fhevm` API exist (cited repeatedly by ?ask sources) but were not fetched; needed only if we use Foundry.
12. **Gas cost of `FHE.checkSignatures`** (verification of threshold sigs on-chain) — no figures documented; affects finalize-tx gas budgeting for multi-value epochs.

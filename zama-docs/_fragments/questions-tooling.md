# Unresolved questions — tooling (after ?ask attempts, 2026-07-03)

1. **Does the Sepolia testnet relayer require an API key?** Docs conflict. `sdk/guides/authentication.md` states "The relayer requires an API key for every request", and two ?ask runs against it answered "Yes" for Sepolia (`_raw/ask/ask-relayer-rate-limits.md`, `_raw/ask/ask-testnet-api-key.md`). But an earlier ?ask against the addresses page answered "**No** — the Sepolia testnet relayer is open, SDK presets work with no `auth`" (`_raw/addresses-examples/ask-03-relayer-url.md`), the API-key page scopes keys to "Zama's hosted Relayer service on mainnet", and the hardhat-plugin README requires `ZAMA_FHEVM_API_KEY` only "on Ethereum mainnet". **Resolution path:** empirically hit `https://relayer.testnet.zama.org` keyless (e.g. run the template's Sepolia test); if 401/403, apply via the form. Build the backend proxy either way.

2. **What are the relayer rate limits (req/s, per-key quotas)?** Not published anywhere in the docs; ?ask returned "I cannot find any published relayer rate limits" (`_raw/ask/ask-relayer-rate-limits.md`). Ask on Discord/community forum; design the frontend to tolerate 429s/backoff. (For the Developer Program, unknown whether the shared testnet relayer throttles per-IP.)

3. **How do you get a *testnet* API key (if one is required)?** ?ask: "I cannot find any docs page that defines a separate 'testnet key' flow" — the only documented process is the mainnet application form (`_raw/ask/ask-testnet-api-key.md`).

4. **Can the SDK v3 `cleartext()` relayer talk to a chain run by `npx hardhat node` + `@fhevm/hardhat-plugin` mock?** `local-development.md` says cleartext mode is for "any chain where you deploy FHEVM contracts in cleartext mode" and that `executorAddress` is "the deployed `CleartextFHEVMExecutor` contract address from your Hardhat setup"; the `hardhat` chain preset ships defaults. But no doc states that the hardhat-plugin's mock environment deploys `CleartextFHEVMExecutor` at that preset address (the plugin deploys its own precompiled FHEVM host contracts). Needs an experiment: start the template node, then run an SDK v3 `cleartext()` script against `hardhat` preset and see if encrypt/decrypt works.

5. **Current CDN path/version for the legacy relayer-sdk browser bundle.** Archived v0.10 docs pin `https://cdn.zama.ai/relayer-sdk-js/0.2.0/…`; npm latest is 0.4.4. No current doc gives an updated CDN URL. If using the legacy SDK in-browser, prefer npm `@zama-fhe/relayer-sdk/bundle` at a pinned 0.4.x.

6. **Timeline/deprecation plan for `@zama-fhe/relayer-sdk`.** npm shows no `deprecated` flag; docs call it "legacy" and point new work to `@zama-fhe/sdk`. The hardhat plugin still peer-depends on relayer-sdk `^0.4.x`, and relayer-sdk `main` is at 0.5.0-rc.1 (async HTTP API migration in progress, `_repos/relayer-sdk/API_MIGRATION.md`). Whether 0.5.0 changes relayer endpoints/behavior for existing dApps is unknown — pin `^0.4.4`.

7. **Does the plugin's `fhevm.publicDecrypt` work when running against `--network sepolia`** (i.e., the same test code exercising the real relayer's public decryption), and what latency/timeout should the lending protocol's reveal tests budget? Docs show mock usage; the Sepolia template test only exercises `userDecryptEuint`. Assumed yes (same API), but untested — verify on Sepolia with generous timeouts.

8. **HCU (FHE gas) budget enforcement on Sepolia vs mock** — `fhevm.computeTransactionHCU(receipt)` exists in the plugin for mock-side estimation; whether/what HCU cap Sepolia enforces per tx is covered in the protocol docs (see `_raw/ask/ask-hcu-limits.md` from the other workstream), not in the tooling docs; cross-check when sizing lending operations (many FHE ops per tx).

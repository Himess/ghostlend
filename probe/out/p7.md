### P7 — Relayer auth reality (Sepolia)

Relayer base URL: `https://relayer.testnet.zama.org`.

**Does keyless dev work today?** YES — createEncryptedInput, publicDecrypt (2841ms), and userDecryptEuint all succeeded in P4/P5/P6 with no API key set.

Raw unauthenticated HTTP probes (no `x-api-key`):
- GET /v1/keyurl → HTTP 404 :: 
- GET /v1/keys → HTTP 404 :: { "message":"no Route matched with those values", "request_id":"e5107c74fb09791d3b57ea369fe7cb39" }
- GET / → HTTP 404 :: { "message":"no Route matched with those values", "request_id":"a50b450604f46c2521436f51c665f312" }

**Decision:** the Sepolia testnet relayer is usable **keyless** for development right now (resolves the docs contradiction in favor of "open on testnet"). Build the backend proxy for mainnet regardless (mainnet relayer requires a key).
